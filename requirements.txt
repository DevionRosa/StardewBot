# Runtime dependencies for `python run_ui.py` and `python app.py`.
#
# The desktop widget uses these directly:
#   - PySide6           : borderless translucent window + QPainter animations
#   - openwakeword      : "hey farmer" wake word
#   - PyAudio           : microphone input
#   - vosk              : local STT
#   - pyttsx3           : local TTS (SAPI5 on Windows)
#   - numpy             : RMS audio level for the listening ripple
#   - requests          : Ollama HTTP + live wiki fetch (chat tiers 2/3)
#   - beautifulsoup4    : wiki infobox parsing
#
pywin32==306; sys_platform == "win32"

PySide6==6.9.1
pyttsx3==2.99
PyAudio==0.2.14
vosk==0.3.45
openwakeword==0.6.0
numpy==2.4.0
requests==2.33.0
beautifulsoup4==4.12.3
